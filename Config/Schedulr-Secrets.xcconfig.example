// Example config. Duplicate to Config/Schedulr-Secrets.xcconfig and fill values.
SUPABASE_URL = https://YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY = YOUR-ANON-KEY
// Never include service role in client builds. Keep empty.
SUPABASE_SERVICE_ROLE_KEY =

// Optional: explicitly set Info.plist path if needed
INFOPLIST_FILE = Schedulr/Info.plist

