// Example config. Duplicate to Config/Schedulr-Secrets.xcconfig and fill values.
SUPABASE_URL = https://YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY = YOUR-ANON-KEY
// Never include service role in client builds. Keep empty.
SUPABASE_SERVICE_ROLE_KEY =

// OAuth callback scheme used for Supabase deep links. Must match Info.plist URL Types and your Supabase dashboard redirect.
SUPABASE_OAUTH_CALLBACK_SCHEME = schedulr

// Google placeholders (only needed if integrating GoogleSignIn SDK directly; safe to leave set).
GOOGLE_CLIENT_ID = YOUR-CLIENT-ID.apps.googleusercontent.com
GOOGLE_REVERSED_CLIENT_ID = com.googleusercontent.apps.YOUR-CLIENT-ID

// Optional: explicitly set Info.plist path if needed
INFOPLIST_FILE = Schedulr/Info.plist
